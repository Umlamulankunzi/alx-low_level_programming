#include "main.h"

/**
 * main - function to test function positive_or_negative(int) corrent output
 *
 * Return: 0 if successful
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
