#include "main.h"

/**
* main - Main Entry point of program
*
* Return: 0 if successful
*/
int main(void)
{
	return (0);
}

/**
* _abs - calculate absolute value on n
(* @n: int
*
* Description: value to calculate absolute value of)?
* Return: absolute value of n 
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-1 * n);
}
