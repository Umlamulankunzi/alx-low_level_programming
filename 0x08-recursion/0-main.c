#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	_puts_recursion("Puts with recursion");
	_puts_recursion("The quick brown fox");
	_puts_recursion("jumped over the lazy dog");
	return (0);
}
